# PR-Style Review — Cycle 1/3

**Issue**: iteration-2
**Date**: 2026-04-26
**Diff range**: 98e2910..HEAD
**Changed files**: 9 files (Cargo.toml, src/ai_invoke.rs, src/ai_title.rs, src/app.rs, src/config.rs, src/hooks.rs, src/main.rs, src/runtime.rs, src/ui.rs)

## Overall Verdict: REQUEST_CHANGES

## Agent Results Summary

| Agent          | Blocking | Warnings | Verdict           |
| -------------- | -------- | -------- | ----------------- |
| Security       | 4        | 8        | REQUEST_CHANGES   |
| Robustness     | 5        | 8        | REQUEST_CHANGES   |
| Quality        | 3        | 6        | REQUEST_CHANGES   |
| Goal Alignment | 4        | 4        | REQUEST_CHANGES   |
| **Total**      | **7**    | **26**   | **REQUEST_CHANGES** |

## Blocking Issues

### B1 (3 reviewers): `ai_title_enabled` not initialized from config
**File**: src/app.rs:536
**Fix**: `ai_title_enabled: config.features.ai_title` (capture before struct literal)

### B2 (2 reviewers): feature toggle q/Enter doesn't sync `ai_title_enabled`
**File**: src/app.rs handle_feature_toggle_key
**Fix**: After applying pending, set `self.ai_title_enabled = self.config.features.ai_title`

### B3 (2 reviewers): `HookEvent` useless round-trip string→enum→string→match
**File**: src/hooks.rs:133-143
**Fix**: Pass `HookEvent` as enum through `AppEvent::HookReceived`, or remove enum entirely

### B4 (2 reviewers): `pane_output_rings` entry inserted before pane existence check
**File**: src/app.rs:2267
**Fix**: Move ring insertion inside workspace pane lookup guard

### B5 (security): `claude --print prompt` argument injection vector
**File**: src/ai_invoke.rs:30-31
**Fix**: Pass prompt via stdin with `--print -` flag, use `.stdin(Stdio::piped())`

### B6 (robustness): `bind_with_retry` calls blocking `UnixStream::connect` on async thread
**File**: src/hooks.rs:91-108
**Fix**: Wrap the blocking connect in `tokio::task::spawn_blocking`

### B7 (alignment): `runtime.handle()` uses `.expect()` — panic in production
**File**: src/runtime.rs:30
**Fix**: Return `Option<Handle>` or `Result<Handle>`

## Next Cycle Directives (Cycle 2)

1. Fix `ai_title_enabled` initialization: read from `config.features.ai_title` before struct move
2. Fix feature toggle dialog: sync `ai_title_enabled` on q/Enter confirm
3. Fix `HookEvent` round-trip: carry enum through `AppEvent::HookReceived` or remove enum
4. Fix `pane_output_rings` insertion: only insert when pane confirmed in workspace
5. Fix `claude --print`: use stdin piping instead of CLI arg for prompt
6. Fix `bind_with_retry`: wrap blocking `UnixStream::connect` with `spawn_blocking`
7. Fix `runtime.handle()`: eliminate `.expect()`, return `Option<Handle>`
